# Status

Updated {{date}}.

This file says where the project **is**. How it got here moves to
`docs/history/phase-<n>.md` when a phase closes, whole.

## Where the project is

| Phase | State |
| --- | --- |
| 0 — Skeleton | Not started. |

`git log --oneline origin/main..main` is the authority for what is unpushed. Do
not quote a commit count here: a number whose far end keeps moving goes stale.

## Next action

{{one line}}
